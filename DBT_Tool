DBT (Data Build Tool) is a open source command line tool that helps data analyst and engineers transform data in their DWH more effectively
It allows users to write modular SQL queries, which are then compiled into raw SQL and run against the database.

